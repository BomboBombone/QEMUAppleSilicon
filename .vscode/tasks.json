{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Configure",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build && ../configure --target-list=aarch64-softmmu,x86_64-softmmu --enable-lzfse --enable-debug --disable-werror && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}

{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Configure",
            "type": "shell",
            "command": "cd ${workspaceFolder}/build && ../configure --enable-lzfse --disable-werror --enable-debug && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
